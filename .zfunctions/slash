lastword=${LBUFFER##* }
lastword=${lastword#}
lastword=${(j:/:)${(s::)lastword}}
[[ $lastword = //* ]] && lastword=$lastword[2,-1]
LBUFFER="${LBUFFER% *} $lastword"
CURSOR=$#LBUFFER

zle slashcomplete

# vim: ft=zsh
